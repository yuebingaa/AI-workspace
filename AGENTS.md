# AgentCanvas 项目协作约定

## 项目入口

- 主要网站源码位于 `site/`。
- 开始网站开发、运行、发布或排障前，必须先完整阅读 `site/STABLE-RUNTIME.md`。
- 所有 `npm` 命令默认在 `site/` 目录执行。

## 每次任务的修改记录

- 网站视觉设计遵循 [当前视觉规范](./site/docs/visual-design.md)：用户确认使用简洁浅色、黑白灰和略暖底色；界面改动同步维护规范中的当前状态，并在下方统一任务日志记录实际改动与验收。

- 从 2026-09-13 起，每次任务都必须在根目录 [TASK-LOG.md](./TASK-LOG.md) 追加记录；开始工作前阅读最近记录，交付前完成本次记录。
- 记录日期和任务目标、实际修改内容及文件、验证方法与结果、启用/发布状态、未完成事项；没有代码修改时明确说明“仅研究 / 文档修改”或“无文件修改”。
- 只记录本次实际执行和验证的内容；未运行的检查说明原因，不能把设计、旧测试或其他并行任务的结果写成本次完成。
- 保留历史记录；本次任务期间可补全本次条目，后续更正应追加说明，不覆盖其他任务记录。记录中不写入密钥、凭据或原始敏感数据。
- 涉及 Agent 架构变化时，仍须按下方约定同步架构文档；任务记录链接到专项文档，不替代专项维护。
- 最终回复简要说明修改结果，并提供本次任务记录的链接。

## Agent 架构文档维护

- Agent 架构的唯一维护入口为 `site/docs/architecture/agent-architecture.md`。修改 Agent 相关代码前先阅读此文档。
- 每次调整 Agent 角色、委派/调度、工具与 Skill、上下文/记忆、证据/验证、权限、预算、事件协议、Notebook 或外部能力接入时，必须在同一次工作中同步更新该文档。
- 文档必须说明当前实现、模块和接口、运行开关、能力边界、验证结果，并在变更记录中追加日期、修改内容和影响。
- 必须区分“源码已实现”“已启用/已发布”和“规划中”，不能把设计图当作上线证明。
- 更新正文后运行 `npm run docs:agent:sync` 更新源码指纹，再运行 `npm run docs:agent:check`。指纹同步不能替代正文和变更记录更新；测试和构建会自动检查指纹是否过期。
- 旧设计草案和导出框图是历史参考；架构变化时以维护入口为准，需要交付新图时从最新文档重新生成。

## 本机网站运行约定

- 稳定站固定使用 `http://127.0.0.1:3000`，对应最近一次验收通过的独立生产构建。
- 开发站固定使用 `http://127.0.0.1:3001`，对应当前 `site` 源码和热更新环境。
- 截图服务健康地址为 `http://127.0.0.1:3198/health`。
- 必须使用 `npm run site:*` 命令检查和管理服务；操作前先运行 `npm run site:status`。
- 不要在 3000 或 3001 上另外启动 `npm run dev`，不要自行换端口。
- 不要使用按名称批量结束 Node 进程的命令，也不要因为测试、构建或任务结束而停止已有网站服务。
- 除非用户明确要求，不得停止稳定站、开发站或截图服务。
- `localhost`、`127.0.0.1`、3000 和 3001 的浏览器存储互不共享；日常访问使用上面的固定 IPv4 地址。

## 常用命令

```text
npm run site:status
npm run site:logs
npm run site:start -- dev
npm run site:restart -- stable
npm run site:publish
npm test
npm run build
```

发布、回退、运行目录、持久化、日志、故障恢复和首次安装的完整规则，以 `site/STABLE-RUNTIME.md` 为准。不得绕过其中的安全约束直接操作后台进程或运行目录。
