# 本机后台运行验收记录

验收日期：2026-09-09。未调用真实 AI 模型，未部署到公网。

当前稳定版：`2026-09-09T06-17-07-261Z-36adf84e`。
保留上一版：`2026-09-09T06-02-44-789Z-4632167a`。
Windows 计划任务：`AgentCanvas-baf9eb00a4`。

| 检查 | 实际结果 |
| --- | --- |
| 独立生产构建 | 两次构建成功，候选健康、持久化、首页及静态资源通过 |
| 源码隔离 | 临时 CSS 变量只出现在开发站；稳定站变量为空，PID 不变；测试变量已移除 |
| 构建失败保护 | 临时错误路由使构建按预期失败；稳定 PID 前后均为 `36096`，版本不变，健康 `ok`；错误路由已移除 |
| 截图服务崩溃 | 约 3.0 秒恢复；稳定站和开发站 PID 未变 |
| 稳定站崩溃 | 约 2.5 秒恢复；开发站 PID 未变 |
| 守护进程崩溃 | Windows 自动拉起新守护进程，两个站点约 30 秒内恢复，无需保留启动命令会话 |
| 回退 | 成功切回上一版，再切回最新版本，均经过候选检查 |
| 页面复查 | 两站 HTTP 200，无页面 JavaScript 运行错误，无残留测试样式 |
| 实际持久化 | 两站 `/api/health` 的 `persistence.configured=true`，启动/运行错误均为 0 |
| 管理器自动化测试 | 2 项通过，覆盖原子配置、日志脱敏、独立恢复、重启后数据保留、重复守护拒绝、降级状态不反复重启、发布中断恢复和手动停止 |
| 静态检查 | 新增运行脚本、开发脚本和 Vite 配置的 ESLint 检查通过 |

故障测试中的秒数是本机本次测量，不是恢复时间保证。任务为当前用户登录模式；机器重启/重新登录的触发器已经配置，但没有为验收重启整台电脑。正式发布采用停止旧写入实例后启动新实例，会有短暂中断。

证据文件（被 Git 忽略，保留在本机）：

- [浏览器源码隔离](./.runtime/runtime-browser-isolation.json)
- [实机故障恢复](./.runtime/runtime-fault-verification.json)
- [构建失败保护](./.runtime/runtime-failed-build-verification.json)
- [最终浏览器检查](./.runtime/runtime-browser-verification.json)
- [稳定站截图](./.runtime/runtime-3000.png)
- [开发站截图](./.runtime/runtime-3001.png)

操作方式及限制见 [稳定运行说明](./STABLE-RUNTIME.md)。

## 2026-09-10：命令行弹窗修复

已将原每分钟启动 PowerShell 的保活方式迁移为两个无控制台程序：

- 主任务 `AgentCanvas-baf9eb00a4` 使用 `AgentCanvasHost.exe` 常驻运行，登录启动并在真实失败时重试。
- 检测任务 `AgentCanvas-baf9eb00a4-watchdog` 使用 `AgentCanvasWatchdog.exe` 每分钟短时检查，主任务已运行时返回成功；不再重复触发常驻任务而产生 `0x800710E0`。
- 宿主有进程互斥锁，守护进程有独占管道及握手，合法重复实例正常跳过，陌生管道仍报错。
- 新增脱敏的 `launcher.log`、`watchdog.log`，更新前先编译校验，保留任务 XML 和管理脚本备份。

验证结果：

| 检查 | 结果 |
| --- | --- |
| 自动化测试 | `npm run test:runtime`：5 项通过 |
| 无控制台 | GUI 子系统头校验通过；真实控制台探针经宿主启动后 `GetConsoleWindow()` 为零 |
| 宿主行为 | 重复实例不启动第二个子进程；stdout/stderr 脱敏；真实错误退出码保留 |
| 定时检测 | 独立临时计划任务实测：尊重禁用、拉起已停止任务、跳过运行中实例；测试任务和目录已清理 |
| 更新恢复 | 发现并处理 Windows 映像文件锁释放延迟；补充文件复制重试和已停服务下的中断更新恢复路径 |
| 实际部署 | 最终主任务于 16:27:31 启动；主任务状态 `Running`，返回 `267009`（仍在运行） |
| 第一次自然定时检测 | 16:28:31 执行完成，检测任务状态 `Ready`，退出码 `0` |
| 第二次自然定时检测 | 16:29:32 执行完成，退出码仍为 `0`；至 16:29:52，守护进程及三个服务 PID 均未变化、健康均为 `ok` |
| 浏览器 | 16:29:03，3000 与 3001 都返回 HTTP 200，无页面 JavaScript 运行错误 |
| 版本与数据 | 稳定发布仍为 `2026-09-09T06-17-07-261Z-36adf84e`；未发布网页新代码，未删除网站数据 |

更新期间曾因旧程序文件短暂占用而中断，服务于 16:27:31 重新启动并通过健康检查；此轮不是无中断操作。旧配置及管理器保存在运行目录 `manager-updates/<更新标识>/previous`。没有为验收重启电脑，也未调用真实 AI。
