# AI 分析网站：项目进度与分支协作基线

> 更新时间：2026-09-02（Asia/Shanghai）
> 资料范围：本项目近期验收记录，以及 2026-09-02 对本地与远程 Git 分支、提交历史、工作树、测试和 `site/package.json` 的核对。
> 用途：作为后续顺序开发、任务交接、验收和必要分支协作的共同基线。本文不包含 API 密钥值、真实业务数据或个人信息。

## 1. 项目目标

建设一个名为 **DataCanvas AI** 的 AI 数据产品工作台。用户可以上传和检查数据，通过自然语言让 AI 规划数据处理任务，由确定性工具执行 DataRecipe、生成 ChangeSet 预览、人工确认变更，并将分析结果导出为 Excel。

核心原则：

- AI 负责理解目标、规划步骤和选择工具，不直接运行任意代码或修改正式数据模型。
- AppSpec 是页面正式状态的唯一事实来源。
- 页面写入统一经过 ChangeSet 校验、预览、人工确认、应用、审计和撤销流程。
- 数据处理统一通过 DataRecipe 和受控工具执行。
- API 密钥仅允许服务端读取，禁止进入客户端构建、Git、审计记录或模型上下文。

## 2. 当前已验证的仓库状态

- 仓库：`AI-workspace`
- 主工作树：`D:/Orca-Projects/AI-workspace`
- 当前集成工作树：`C:/Users/Administrator/orca/workspaces/AI-workspace/AI分析网站设计`
- 当前集成分支：`feature/website-redesign`
- 当前已验收功能集成 HEAD（本次文档更新前）：`ad20b945ff5f4077b4adffc6d010863b6dfd33d4`
- 数据所有权集成提交：`563326d68bd82284634c9995903c1a20cf314a6a`
- 工作台 UI 集成提交：`ad20b945ff5f4077b4adffc6d010863b6dfd33d4`
- 远程跟踪：`origin/feature/website-redesign`
- 集成状态：数据分支与 UI 分支均已通过 fast-forward 合入，完整验证通过后已推送；文档更新开始前本地与远程为 ahead/behind `0/0`
- `security/dependency-upgrades`：提交 `9a557f6e2c343d7dbf76457c6a90d38662377032` 已在集成历史中
- `main`：仍保持在初始提交 `134d543`，未修改、未合并当前功能开发

已创建的并行工作树：

| 工作树 | 分支 | 当前起点 | 责任方向 |
|---|---|---|---|
| `AI分析网站设计` | `feature/website-redesign` | `ad20b94` | 当前集成与后续顺序开发 |
| `数据与Harness平台` | `feature/data-harness-platform` | `563326d` | 已合入集成分支，暂时保留作里程碑记录 |
| `工作台UI体验` | `feature/studio-ui-experience` | `ad20b94` | 已合入集成分支，暂时保留作里程碑记录 |
| `依赖安全升级` | `security/dependency-upgrades` | `9a557f6` | 升级已进入集成历史，分支保留作安全里程碑记录 |

## 3. 已完成里程碑与主要提交

| 提交 | 内容 |
|---|---|
| `63f66c7` | 建立 AppSpec 驱动的数据工作室原型 |
| `1b9d092` | 增加 AppSpec/ChangeSet Schema 与组件注册表 |
| `2ba6a8c` | 集成 Puck 可视化编辑器 |
| `8603e97` | 增加数据绑定和编辑器权限 |
| `0f80214` | 增加数据源可观察性、本地持久化并修复 Puck 状态循环 |
| `4cdc87f` | 集成 Schema 安全的 DeepSeek 规划 |
| `0747e65` | 增加可执行 DataRecipe 与字段血缘 |
| `7a0d45c` / `439d814` | 建立有界 DeepSeek Harness 执行流程 |
| `7729344` | 修正 Harness 模型、工具和总执行时间预算 |
| `d11e27e` | 优化复杂 Harness 任务上下文预算与 working memory |
| `52d4f28` | 增加安全的 DataRecipe Excel 导出 |
| `9a557f6` | 升级生产依赖并消除已报告的生产依赖审计问题 |
| `8f2efea` | 增加安全的临时 CSV 数据集接入 |
| `66ad390` | 建立项目进度与分支协作基线文档 |
| `563326d` | 强化 demo ownership 边界，并完成数据、API、Harness 与导出所有权约束 |
| `ad20b94` | 增加可访问的任务与 ChangeSet 历史工作区，并完成组合 UI 验收 |

## 4. 当前系统能力

### 4.1 工作台与页面模型

- 三栏 DataCanvas AI 工作台。
- 页面结构树、桌面/移动预览和页面切换。
- AppSpec 递归渲染与类型化组件注册表。
- Puck 可视化编辑、组件拖拽和属性修改。
- viewer、editor、admin 演示权限。

### 4.2 ChangeSet

- Schema、目标、权限、页面、组件和属性校验。
- 预览、应用、拒绝、撤销和历史记录。
- 正式 AppSpec 与待确认预览隔离。
- localStorage 本地持久化与中文恢复错误。
- 审计记录只保存必要元数据。

### 4.3 数据处理

DataRecipe 已支持：

- 选择、筛选和重命名字段。
- 类型转换和派生字段。
- 分组、聚合、排序和行数限制。
- 输入/输出 Schema、步骤结果、耗时和中文错误。
- 字段血缘追踪、失败定位、局部撤销和结果恢复。

### 4.4 DeepSeek Harness

- 服务器端 DeepSeek 规划，客户端无法读取密钥。
- 状态包括 planning、executingTool、observing、awaitingConfirmation、completed、blocked、failed 和 cancelled。
- 支持任务复杂度分级、模型/工具次数、字符、token 和执行时间预算。
- 使用结构化 working memory，避免旧观察在后续轮次重复膨胀。
- 当前工具包括数据集检查、字段检查、配方预览/验证、ChangeSet 预览和 Excel 导出。
- 页面写入停在人工确认，不自动修改正式 AppSpec。

### 4.5 Excel 导出

- 依赖：`write-excel-file@4.1.1`，服务端使用 Node 导出入口。
- 导出 DataRecipe 执行后的结果，而非未经处理的原始数据。
- 生成真正的 `.xlsx` Buffer 和受控下载 API。
- 至少包含“分析结果”和“导出说明”工作表。
- 提供文件名清理、路径限制、公式注入防护、容量与超时限制。
- 导出内容不进入 localStorage、模型上下文或 ChangeSet 审计；审计只保存文件元数据。

### 4.6 CSV 临时数据集接入

- 服务端使用 `csv-parse@7.0.2`，解析器不进入客户端产物。
- 支持 UTF-8、UTF-8 BOM、引号、字段内逗号和换行。
- 处理空表头、重复表头、类型推断和字段映射。
- 提供空值、唯一值、重复行、类型冲突和异常值分析。
- 标记姓名、电话、邮箱、身份证和地址等敏感风险。
- 数据可供查询运行时、图表绑定、DataRecipe、Harness 和 Excel 导出使用。
- 敏感策略确认前，Harness 不向模型提供数据摘要；确认后只提供脱敏样本或排除敏感样本。

### 4.7 Demo ownership 与历史工作区

- 数据集、Harness 幂等命名空间和 Excel 下载均复用服务端确定的 demo ownership 范围。
- `demo-single-user` 是明确标注的演示身份模式，用于约束本地单用户数据归属，不等同于正式认证、多租户授权或生产级隔离。
- 任务与 ChangeSet 历史面板支持任务状态、工具调用、预算、耗时、阻塞/失败/取消原因和审计轨迹查看。
- 历史弹层支持键盘焦点圈闭、Escape/按钮/遮罩关闭和触发按钮焦点恢复。
- Harness 与 ChangeSet 历史可从当前浏览器 localStorage 恢复；刷新不会自动继续未确认写操作。

## 5. 当前依赖与运行环境

`site/package.json` 的已核对生产依赖：

- Node.js：`>=22.13.0`
- `next@16.3.4`
- `react@19.2.6`
- `react-dom@19.2.6`
- `@puckeditor/core@0.23.0`
- `zod@^4.5.4`
- `write-excel-file@4.1.1`
- `csv-parse@7.0.2`

常用命令（在 `site` 目录执行）：

```powershell
pnpm dev
pnpm test
pnpm lint
pnpm build
pnpm audit --prod
```

本地开发地址通常为：

```text
http://localhost:3000/
```

## 6. 当前验证基线

数据提交 `563326d` 与 UI 提交 `ad20b94` 依次 fast-forward 合入 `feature/website-redesign` 后，集成分支已完成以下验证：

- 自动化测试：29 个测试文件、166 项全部通过。
- TypeScript：`pnpm exec tsc --noEmit` 通过。
- ESLint、生产构建和 `git diff --check` 通过。
- `pnpm audit --prod`：0 个已知漏洞，Info、Low、Moderate、High、Critical 均为 0。
- 首页与 `favicon.svg`：HTTP 200。
- 浏览器组合验收：编辑/预览切换、Puck 加载、数据源字段入口、任务历史弹层、取消原因、焦点圈闭与恢复、localStorage 刷新恢复均通过。
- 浏览器验收期间 React/JavaScript 运行时错误为 0，DeepSeek/AI API 请求为 0。
- 数据提交和 UI 提交均存在于当前集成历史，本地与远程在集成推送后同步。
- `main` 未修改、未合并；两个子分支暂时保留作里程碑记录。

## 7. 安全与隐私约束

每轮开发必须继续遵守：

1. 不读取、打印、复制或提交 `.env.local` 的密钥内容。
2. `.env.example` 只能保留空的密钥占位符。
3. 禁止提交真实 CSV、真实姓名、电话、邮箱、身份证、地址或公司业务数据。
4. 测试数据只能使用明确的虚构值。
5. 禁止提交 `node_modules`、`.next`、`.vinext`、`.wrangler`、`dist`、缓存、临时浏览器目录和构建产物。
6. 每次提交前执行工作区、暂存区和最终提交对象的敏感信息扫描。
7. AI 只能看到经过策略允许的 Schema、摘要或脱敏样本。
8. 写入页面必须经过 ChangeSet 预览和人工确认。
9. 未完成认证与持久化改造前，不公开部署真实业务数据功能。

## 8. 已知限制

- `demo-single-user` 只是服务端固定的演示身份边界，不是真实认证、租户管理或正式多用户隔离。
- 上传数据集和 Excel 临时下载文件仍保存在服务进程内存中；服务重启、TTL 到期或实例切换后会失效。
- 上传数据最长保留 30 分钟、最多 10 个数据集；当前没有数据库或对象存储持久化。
- CSV 仅支持 UTF-8 与 UTF-8 BOM。
- 服务端解析阶段暂不提供逐行进度。
- 任务、ChangeSet 和工作台历史主要保存在当前浏览器 localStorage。
- 当前不支持跨浏览器、跨设备或多用户历史同步。
- 系统尚不适合直接公开为多用户生产服务；正式部署前必须补齐认证、授权、租户隔离和持久化基础设施。
- 最新 CSV/Excel 能力完成了 Mock 和自动化验收，但尚未进行新的真实 DeepSeek 端到端验收。
- `main` 尚未接收功能分支代码，也尚未建立正式发布流程。

## 9. 当前分支协作基线

- `feature/data-harness-platform` 已以提交 `563326d68bd82284634c9995903c1a20cf314a6a` fast-forward 合入集成分支。
- `feature/studio-ui-experience` 已以提交 `ad20b945ff5f4077b4adffc6d010863b6dfd33d4` fast-forward 合入集成分支。
- `security/dependency-upgrades` 的提交 `9a557f6e2c343d7dbf76457c6a90d38662377032` 已在当前集成历史中。
- 两个子分支暂时保留作为已验收里程碑记录，不删除；除非另有明确任务，不继续作为默认开发分支。
- 后续默认回到 `feature/website-redesign`，采用单分支、顺序开发和逐里程碑验收。
- 只有再次出现目标明确、文件重叠低、接口边界可提前约定的并行任务时，才创建新的短期功能分支。
- 新并行分支必须先约定负责人、共享文件规则、集成顺序和验证门槛，并继续禁止直接修改或合并 `main`。

## 10. 推荐的下一阶段

后续默认在 `feature/website-redesign` 顺序推进，优先级建议：

1. 将数据集和导出文件的进程内存仓库替换为可持久化、可清理、可审计的适配器。
2. 增加正式认证、租户、授权和数据集所有权校验，替换 `demo-single-user`。
3. 将工作台、任务与审计历史迁移到服务端存储，支持跨设备同步和并发控制。
4. 完善 CSV 上传进度、编码策略、错误恢复以及数据源生命周期管理。
5. 在受控环境完成 CSV → DataRecipe → Harness → ChangeSet/Excel 的真实端到端验收。
6. 建立部署、监控、备份和安全复核流程；达到发布标准后再单独评审是否合并 `main`。

## 11. 集成与提交检查清单

每个里程碑提交前：

- [ ] 默认位于 `feature/website-redesign`；若使用临时并行分支，工作范围符合事先约定的责任边界。
- [ ] `git diff --check` 通过。
- [ ] 自动化测试、TypeScript、ESLint 和生产构建通过。
- [ ] `pnpm audit --prod` 满足当前无已知漏洞基线。
- [ ] `.env.local` 被忽略且未跟踪。
- [ ] `.env.example` 不包含密钥值。
- [ ] 工作区、暂存区和提交对象敏感信息扫描为 0 命中。
- [ ] 无真实数据、临时文件、缓存或构建产物进入提交。
- [ ] 只推送当前工作分支，不操作 `main`，不自动创建 Pull Request。

默认开发与必要分支集成顺序：

1. 在 `feature/website-redesign` 完成一个小里程碑，验证、提交并推送。
2. 仅当任务明确并行且文件重叠低时，创建短期分支并约定接口与集成顺序。
3. 子分支验收后优先以 fast-forward 合入 `feature/website-redesign`；不满足快进条件时停止并重新评审。
4. 在集成分支执行完整自动化、浏览器、隐私与依赖安全验收。
5. 保持 `main` 不变，直到另行完成发布评审和明确授权。

## 12. 2026-09-02 分支集成记录

1. 进度基线文档首先由提交 `66ad390034967fa1321460568b8c14f8724f5463` 保存。
2. `feature/data-harness-platform` 完成独立验收后，提交 `563326d68bd82284634c9995903c1a20cf314a6a` 以 `--ff-only` 合入 `feature/website-redesign`。
3. `feature/studio-ui-experience` 基于数据提交继续开发，提交 `ad20b945ff5f4077b4adffc6d010863b6dfd33d4` 的唯一直接父提交为 `563326d68bd82284634c9995903c1a20cf314a6a`，随后同样以 `--ff-only` 合入集成分支。
4. 合并后确认数据提交与 UI 提交均在集成历史中，并完成 29 个测试文件、166 项测试、TypeScript、ESLint、生产构建、生产依赖审计和浏览器组合验收。
5. `origin/feature/website-redesign` 已同步到 UI 集成提交；本文档将在此基础上形成独立文档提交。
6. `feature/data-harness-platform` 与 `feature/studio-ui-experience` 暂时保留作为里程碑记录，不删除。
7. `main` 全程未修改、未合并或推送；当前功能仍只存在于功能与里程碑分支。
