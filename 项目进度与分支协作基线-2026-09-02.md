# AI 分析网站：项目进度与执行计划

> 更新时间：2026-09-04（Asia/Shanghai）
> 当前定位：本地或公司内网使用，所有访问者共享 `demo-single-user` 工作空间；禁止公开互联网部署，禁止处理正式敏感数据。
> 资料范围：原始《AI 数据网站-Agent 平台方案》、当前 Git 历史、代码结构、自动化测试与本次验证结果。本文不包含 API 密钥、真实业务数据、个人信息或本地验收文件内容。

## 1. 项目目标

建设 **DataCanvas AI 数据产品工作室**：用户导入或选择数据集，通过自然语言与可视化编辑器完成数据检查、DataRecipe 数据处理、指标与页面生成、ChangeSet 预览和 Excel 导出。

核心原则：

- `AppSpec` 是正式页面状态的唯一事实来源。
- `DataRecipe` 是可执行、可验证、可追溯的数据处理协议。
- AI 只负责规划和调用白名单工具，不执行任意代码，不直接修改正式状态。
- 页面变更统一经过 ChangeSet Schema、权限与目标校验、画布预览、人工确认、应用、审计和撤销。
- 模型上下文只包含必要的 Schema、摘要和脱敏信息，不发送完整数据集。
- 模型密钥只在服务端读取，不进入客户端、Git、日志、审计或评测报告。

## 2. 当前仓库基线

| 项目 | 当前事实 |
|---|---|
| 仓库 | `AI-workspace` |
| 集成工作树 | `C:/Users/Administrator/orca/workspaces/AI-workspace/AI分析网站设计` |
| 集成分支 | `feature/website-redesign` |
| 当前 HEAD | `b68516affd120c85af3ec1902c752a76024936b4` |
| 远程分支 | `origin/feature/website-redesign` |
| 远程同步 | 文档更新开始前 ahead/behind 为 `0/0` |
| 工作区 | 文档更新开始前干净 |
| `main` | 保持在 `134d543`，未合并当前功能开发 |

重要集成提交：

- 安全依赖升级：`9a557f6e2c343d7dbf76457c6a90d38662377032`
- 数据与 ownership 集成：`563326d68bd82284634c9995903c1a20cf314a6a`
- 工作台 UI 集成：`ad20b945ff5f4077b4adffc6d010863b6dfd33d4`
- 项目进度更新：`219213f3cc4ea8a95b598466d63d743ed01f3971`
- Harness 评测第一阶段：`a410b38bda8596227c90b7c1f0bc5a2a394fe6fa`
- Harness 离线评测第二阶段：`629b38ef4be3d7e18bd66838b0beb2a496b39d2e`
- 受控 Live Evaluation Runner：`b68516affd120c85af3ec1902c752a76024936b4`

当前工作树与分支：

| 工作树 | 分支 | 当前提交 | 状态与用途 |
|---|---|---|---|
| `AI分析网站设计` | `feature/website-redesign` | `b68516a` | 当前集成与默认顺序开发分支 |
| `晚间运行` | `feature/night-work` | `b68516a` | 新建的本地试验分支，尚未设置远程上游 |
| `数据与Harness平台` | `feature/data-harness-platform` | `563326d` | 已合入，暂留作里程碑记录 |
| `工作台UI体验` | `feature/studio-ui-experience` | `ad20b94` | 已合入，暂留作里程碑记录 |
| `依赖安全升级` | `security/dependency-upgrades` | `9a557f6` | 已合入，暂留作安全里程碑记录 |

本地验收资料不属于产品源码。当前 `晚间运行` 工作树仍存在未跟踪的本地验收资料目录，因此该工作树尚不应提交。推荐把这类资料统一放到 Git 工作树之外的受控目录，再用 Windows Junction 映射到需要它的工作树，并在本地 Git exclude 中忽略映射路径。不得把真实 Excel、真实个人信息或内部业务规则加入 Git。

## 3. 已完成里程碑

| 里程碑 | 提交 | 状态 |
|---|---|---|
| AppSpec 驱动的三栏工作台原型 | `63f66c7` | 完成 |
| AppSpec/ChangeSet Schema 与组件注册表 | `1b9d092` | 完成 |
| Puck 可视化编辑适配层 | `2ba6a8c` | 完成 |
| 数据绑定、查询运行时与编辑权限 | `8603e97` | 完成 |
| 数据源详情、字段分析、审计和本地持久化 | `0f80214` | 完成 |
| Schema 安全的 DeepSeek 结构化规划 | `4cdc87f` | 完成 |
| 可执行 DataRecipe 与字段血缘 | `0747e65` | 完成 |
| 有界 DeepSeekHarness、工具发现与上下文优化 | `7a0d45c` 至 `d11e27e` | 完成 |
| 安全 DataRecipe Excel 导出 | `52d4f28` | 完成 |
| 生产依赖安全升级 | `9a557f6` | 完成 |
| 安全临时 CSV 数据集接入 | `8f2efea` | 完成 |
| Demo ownership 边界 | `563326d` | 完成 |
| 可访问的任务与 ChangeSet 历史工作区 | `ad20b94` | 完成 |
| 服务端身份边界与确定性评测基础 | `a410b38` | 完成 |
| 11 用例离线 Harness 评测基线 | `629b38e` | 完成 |
| 双重门禁 Live Evaluation Runner | `b68516a` | 基础设施完成，尚待受控真实试运行 |

## 4. 当前产品能力

### 4.1 工作台、AppSpec 与 Puck

- 中文三栏工作台、页面/图层面板、数据产品画布和 AI 构建助手。
- 页面树、桌面/移动预览、响应式布局和类型化组件注册表。
- 页面标题、AI 洞察、指标卡、指标卡组、柱状图、数据健康度和数据表格等组件。
- Puck 编辑/预览切换、选择、排序、添加、删除和属性编辑。
- Puck 只维护编辑草稿；正式状态仍由 AppSpec 和 ChangeSet 流程管理。

### 4.2 ChangeSet 与权限

- 支持 Schema、组件属性、页面目标、节点 ID、页面容量和角色权限校验。
- 支持预览、应用、取消、拒绝、撤销和审计。
- 待确认预览与正式 AppSpec 隔离，AI 不会自动应用写操作。
- 公共 Planner/Harness 请求使用 strict Schema，拒绝客户端提交的 `role`、`tenantId`、`ownerId` 和 `userId`。
- `demo identity` 与服务端 `editor` 身份由服务端注入；前端角色切换仅用于 UI 演示，不参与服务端授权。

### 4.3 数据绑定、DataRecipe 与字段血缘

- 指标、图表和表格由数据绑定计算，不再依赖固定演示值。
- 本地查询支持聚合、分组、筛选、排序和数量限制。
- DataRecipe 支持选择、筛选、重命名、类型转换、派生、分组聚合、排序和限制行数。
- 每一步输出字段 Schema、行数、耗时、中文错误与字段血缘。
- 非法字段、非法转换、除零、空值和重复步骤会安全失败，不破坏正式 AppSpec。

### 4.4 数据源与 CSV

- 保留 `retail_orders` 虚构 fixture 作为演示和回退数据。
- 服务端使用 `csv-parse@7.0.2` 流式解析 UTF-8/UTF-8 BOM CSV。
- 支持引号、字段内逗号和换行、空/重复表头、类型推断和字段名映射。
- 提供字段质量、空值、唯一值、重复行、类型冲突、异常值和敏感字段风险提示。
- 上传数据集进入带容量和 TTL 限制的服务端内存仓库，可供查询、DataRecipe、Harness、图表和导出使用。
- 原始完整数据不进入 localStorage、ChangeSet 审计或模型上下文。

### 4.5 DeepSeekHarness

- 项目内自有的服务端 `DeepSeekHarness` 状态机，不在客户端运行 Agent 循环。
- 状态包括 `planning`、`executingTool`、`observing`、`awaitingConfirmation`、`completed`、`blocked`、`failed` 和 `cancelled`。
- 白名单工具包括数据集检查、字段检查、配方预览/验证、AppSpec 检查、ChangeSet 预览和 Excel 导出。
- 支持任务复杂度分级、动态工具暴露、结构化 working memory、幂等请求和可恢复任务摘要。
- 模型/工具次数、字符、token、模型超时、工具超时和实际执行时间都有独立预算。
- 工具或字段缺失时进入 `blocked`；写操作只生成待确认 ChangeSet。

### 4.6 Excel 导出

- 使用 `write-excel-file@4.1.1` 的服务端 Node 入口在内存中生成 `.xlsx` Buffer。
- 只导出已验证 DataRecipe 的结果，包含“分析结果”和“导出说明”工作表。
- 保留数字、文本和日期类型，并显式防护公式注入。
- 文件名、行列数、大小、超时和下载 token 均受控。
- 导出内容不进入模型上下文、localStorage 或审计正文；审计只保存必要元数据。

### 4.7 历史、审计与本地持久化

- 当前 DataProduct、正式 AppSpec、ChangeSet 历史、Harness 摘要、查询记录和审计可在浏览器本地恢复。
- 载入本地数据时重新进行 Schema 校验；损坏或版本不兼容时回退安全 fixture。
- 任务历史和 ChangeSet 历史面板支持状态、工具、预算、耗时、取消/阻塞/失败原因与键盘可访问性。
- 刷新不会自动继续未确认写操作。

## 5. AI Harness 评测基线

### 5.1 离线 Mock 基线

离线 suite 为 `harness-baseline-v1`，共 11 个确定性用例：

- 数据集摘要与字段质量检查。
- 华东异常订单配方预览。
- 收入标题 ChangeSet 预览。
- 复购率指标 ChangeSet 预览。
- 配方预览后 Excel 导出。
- 缺少复购字段时安全阻塞。
- 未知工具、绕过确认和身份伪造拦截。
- 数据与密钥外泄提示词防护。

当前 Mock 结果：11/11 通过，质量分 100/100，安全硬门全部通过；工具精确率、召回率、顺序与 ChangeSet 目标检查均为 100%，AppSpec 意外修改率和 canary 泄露率均为 0%。这些结果只证明评测器、脚本模型、Harness 执行器和安全边界按预期工作，不代表真实模型准确率。

### 5.2 Live Evaluation Runner

`harness-live-smoke-v1` 已实现，但默认不会运行真实模型：

- 独立命令：`pnpm test:eval:live`；普通 `pnpm test` 和 `pnpm test:eval` 始终为离线 Mock。
- 必须同时启用 Runner 与本地服务端 Live 开关，并通过仅存在于父子进程内存中的随机 nonce。
- 只允许 loopback HTTP，拒绝非本机地址、URL 凭据和外部重定向。
- 首批 manifest 固定为 3 个用例：数据集摘要、华东配方预览、收入标题待确认预览。
- 全局上限：7 次模型调用、12,000 prompt tokens、3,000 completion tokens、180 秒主动执行时间、0 次自动重试。
- 每个用例启动前预留最坏预算；provider usage 异常、HTTP/认证/限流/超时/Schema/安全硬门失败时立即停止。
- 报告只向 stdout 输出统一的脱敏 JSON 与 Markdown 摘要，不保存原始 prompt、响应、reasoning、Authorization、nonce、完整事件或 ChangeSet 正文。

Live Runner 已通过 Mock、门禁、预算、nonce、loopback、报告脱敏和子进程清理测试。本次文档更新没有运行 Live 模式，也没有发起真实 DeepSeek 请求。

## 6. 当前技术与验证基线

生产依赖：

- Node.js `>=22.13.0`
- Next.js `16.3.4`
- React / React DOM `19.2.6`
- Vinext `1.0.0-beta.3`
- Puck `0.23.0`
- Zod `^4.5.4`
- `csv-parse@7.0.2`
- `write-excel-file@4.1.1`

2026-09-04 在 HEAD `b68516a` 上完成的只读验证：

- `pnpm test`：33 个测试文件、272 项测试全部通过。
- `pnpm test:eval`：1 个测试文件、25 项测试通过；11 个离线用例全部通过。
- TypeScript：`pnpm exec tsc --noEmit --incremental false` 通过。
- ESLint：通过。
- Vinext 生产构建：通过。
- `pnpm audit --prod`：0 个已知漏洞。
- 本次没有运行真实 Live 评测，没有调用真实 DeepSeek。

由于当前 PowerShell PATH 未直接暴露 `pnpm`，本次使用现有 Corepack 执行 `corepack pnpm ...`；没有安装或升级依赖。

## 7. 当前安全边界

当前系统只适合本地或受控公司内网演示：

1. 所有访问者共享同一服务端生成的 `demo-single-user` 身份和工作空间。
2. 没有员工级账号、会话、租户隔离或正式 RBAC；前端 viewer/editor/admin 切换不是安全边界。
3. 不允许公开互联网部署，不允许处理正式敏感数据。
4. 客户端提交的身份字段不能参与授权；服务端仍必须拒绝未知和伪造身份字段。
5. `.env.local`、真实 CSV/XLSX、个人信息、业务规则和临时浏览器目录不得进入 Git。
6. 本地验收资料应存放在仓库外；如果通过 Junction 给多个工作树共享，Git 中仍必须保持忽略和未跟踪状态。
7. 真实 DeepSeek 只允许通过显式 Live 门禁与固定预算运行，普通测试和开发流程不得触发。

以下任一条件发生时，必须重新启动正式认证与持久化计划：

- 允许互联网访问或面向外部客户开放。
- 不同员工或客户需要数据隔离。
- 开始处理正式个人信息、商业敏感数据或受监管数据。
- 需要跨设备同步、长期保留、审计取证、撤销下载或多人协作。
- 部署到多实例、无状态或第三方云运行环境。

## 8. 已知限制

- `demo-single-user` 不是真实认证、多租户授权或正式用户隔离。
- 数据集与 Excel 下载文件仍在服务进程内存中，重启、TTL 到期或实例切换后会失效。
- 工作台、任务和 ChangeSet 历史主要保存在当前浏览器 localStorage，不支持跨设备同步。
- CSV 仅支持 UTF-8 与 UTF-8 BOM；服务端解析暂不提供细粒度逐行进度。
- 尚无数据库、对象存储、备份、正式发布、回滚和跨实例幂等基础设施。
- 当前 DeepSeekHarness 是项目内受控实现；尚未完成 Microsoft Harness 对照适配，也没有通用 `AgentRuntimeAdapter` 的多供应商切换。
- Live Evaluation Runner 只是受控真实试运行基础设施，尚未形成真实模型准确率基线。
- 原始方案中的 Data Formulator 正式适配层、公开分享、发布版本和访客问答仍未完成。
- `main` 未接收功能分支代码，项目尚无正式发布流程。

## 9. 更新后的实施计划

### 里程碑 P0：整理本地验收资料与工作树边界

目标：让集成分支和 `feature/night-work` 都能安全使用本地验收资料，但 Git 永远不追踪这些文件。

- 把本地验收资料移到 Git 工作树之外的固定受控目录。
- 在需要的工作树中创建指向同一目录的 Junction，不复制真实文件。
- 使用本地 exclude 忽略映射路径，并分别确认各工作树 `git status` 干净。
- 为后续自动化准备完全虚构、可提交的最小 fixture；真实资料只用于人工本地验收。

完成标准：所有工作树均无验收资料的 untracked/modified 状态；切换或删除工作树不会造成资料丢失；Git 历史中不存在真实文件。

### 里程碑 P1：首次受控 Live Harness 小规模评测

目标：用已经实现的 Live Runner 获取第一份真实模型基线，不修改正式 AppSpec。

- 仅运行 manifest 中的 3 个固定用例，每个用例只运行 1 次。
- 严格执行 7 次模型调用、12,000 prompt tokens、3,000 completion tokens、180 秒主动执行、0 重试的全局预算。
- 核对工具选择、顺序、终态、token、耗时、ChangeSet 精确目标和全部安全硬门。
- 将 Live 结果与离线 Mock 使用同一指标口径比较，但不覆盖离线基线。
- 如果任一安全硬门失败，停止后续真实调用，先以 Mock 回归修复。

完成标准：3 个用例全部满足终态和工具约束；正式 AppSpec 不变；泄露、身份覆盖和非法写入均为 0；形成可复现的脱敏结论。

### 里程碑 P2：业务验收场景基线

目标：把“异常订单整理、复购分析、Excel 导出”转成不含真实数据的可重复验收规范。

- 从本地业务验收材料中只提取字段契约、规则说明和期望结果形状，不复制真实内容到 Git。
- 创建虚构 fixture 和期望输出，覆盖异常识别、配方执行、字段血缘、复购指标与 Excel 导出。
- 增加离线 Harness 用例与浏览器 Mock 验收，明确工具顺序和 ChangeSet/导出约束。
- 评估当前 DataRecipe 是否缺少日期窗口、去重、关联或更复杂异常规则；缺少能力时先扩充确定性执行器。

完成标准：虚构数据能够完整复现业务规则；自动化测试不依赖本地真实文件；导出与正式 AppSpec 相互隔离。

### 里程碑 P3：内部使用稳定性

目标：在保持共享 `demo-single-user` 的前提下，提高公司内网使用的稳定性。

- 为 DatasetRepository、Harness 任务、审计和 ExcelExportStore 增加可替换的本地持久化适配器设计。
- 增加进程重启恢复、TTL 清理、容量告警、备份与恢复测试。
- 完善 CSV 上传进度、错误恢复和数据源生命周期管理。
- 建立启动、停止、健康检查、日志脱敏和故障排查说明。

完成标准：服务重启后必要状态可恢复；清理策略可验证；内部部署仍明确禁止正式敏感数据与外网开放。

### 延期里程碑：正式认证与多用户基础设施

以下内容保留在架构计划中，但当前不实施、不安装相关依赖：

- Supabase Auth/PostgreSQL/Storage/RLS，或 Auth.js + PostgreSQL + S3/R2 的正式选型。
- 真实用户登录、会话、退出、租户、membership 和 RBAC。
- 数据集、任务、审计和导出文件的用户级隔离、配额、撤销与对象存储。
- 跨设备同步、公开分享、正式发布和生产运维。

只有第 7 节列出的触发条件出现并重新完成安全设计评审后，才启动该里程碑。

## 10. 分支协作计划

- 默认继续在 `feature/website-redesign` 进行顺序开发和逐里程碑验收。
- `feature/night-work` 只用于明确的本地试验；开始编码前必须先清理未跟踪验收资料，并约定可提交文件范围。
- 只有任务并行、文件重叠低且接口边界清楚时才新建短期分支。
- 子分支合入前先确认共同基线，优先 `git merge --ff-only`；不能快进时停止并重新评审。
- 禁止自动修改、合并或推送 `main`；发布评审前不创建面向生产的合并动作。
- 已合入的历史分支暂时保留为里程碑记录，不在没有明确授权时删除。

## 11. 每个里程碑的完成门禁

- [ ] `git diff --check` 通过。
- [ ] 全部自动化测试、TypeScript、ESLint 和生产构建通过。
- [ ] `pnpm test:eval` 通过；Mock 结果不得描述为真实模型准确率。
- [ ] `pnpm audit --prod` 保持 0 个已知漏洞。
- [ ] `.env.local` 被忽略且未跟踪，`.env.example` 不包含密钥值。
- [ ] 工作区、暂存区和提交对象敏感信息与禁止路径扫描为 0 命中。
- [ ] 无真实 CSV/XLSX、个人信息、内部业务文件、报告、缓存或构建产物进入提交。
- [ ] 页面写入仍经过 ChangeSet 预览和人工确认，正式 AppSpec 无意外修改。
- [ ] 只推送当前工作分支，不 force push、不操作 `main`、不自动创建 Pull Request。
- [ ] 真实 DeepSeek 调用必须单独授权，并使用 Live Runner 的显式门禁和预算。

## 12. 当前结论

原始方案中“数据处理 → AI 工具调用 → ChangeSet 预览 → 人工确认 → 页面/Excel 交付”的阶段 A 核心闭环已经在本地 fixture、临时 CSV、Puck 和项目内 DeepSeekHarness 上成立，并具备可重复的离线评测与受控 Live 评测入口。

项目当前应继续保持 **内部共享、非公开、非敏感数据** 的定位。下一步不是扩大功能面，而是先整理本地验收资料边界、完成首次小规模 Live 评测，再用虚构 fixture 固化真实业务验收场景。正式认证、数据库和对象存储方案保留，但延期到部署边界或数据隔离需求发生变化时再启动。
