# AI 分析网站：项目进度与分支协作基线

> 更新时间：2026-09-02（Asia/Shanghai）  
> 资料范围：本项目近期对话中的验收记录，以及 2026-09-02 对本地 Git 分支、提交历史、工作树和 `site/package.json` 的只读核对。  
> 用途：作为后续并行开发、任务交接、验收和合并的共同基线。本文不包含 API 密钥、真实业务数据或个人信息。

## 1. 项目目标

建设一个名为 **DataCanvas AI** 的 AI 数据产品工作台。用户可以上传和检查数据，通过自然语言让 AI 规划数据处理任务，由确定性工具执行 DataRecipe、生成 ChangeSet 预览、人工确认变更，并将分析结果导出为 Excel。

核心原则：

- AI 负责理解目标、规划步骤和选择工具，不直接运行任意代码或修改正式数据模型。
- AppSpec 是页面正式状态的唯一事实来源。
- 页面写入统一经过 ChangeSet 校验、预览、人工确认、应用、审计和撤销流程。
- 数据处理统一通过 DataRecipe 和受控工具执行。
- API 密钥仅允许服务端读取，禁止进入客户端构建、Git、审计记录或模型上下文。

## 2. 当前已验证的仓库状态

- 仓库：`AI-workspace`
- 主工作树：`D:/Orca-Projects/AI-workspace`
- 当前集成工作树：`C:/Users/Administrator/orca/workspaces/AI-workspace/AI分析网站设计`
- 当前集成分支：`feature/website-redesign`
- 当前稳定提交：`8f2efea7bbd190214ff6e2d6968c56057c180b8a`
- 远程跟踪：`origin/feature/website-redesign`
- 当前工作区：记录创建前为干净状态，本地与远程同步
- `main`：保持在初始提交 `134d543`，尚未合并当前功能开发

已创建的并行工作树：

| 工作树 | 分支 | 当前起点 | 责任方向 |
|---|---|---|---|
| `AI分析网站设计` | `feature/website-redesign` | `8f2efea` | 集成与整体验收 |
| `数据与Harness平台` | `feature/data-harness-platform` | `8f2efea` | 数据、API、Harness、服务端工具 |
| `工作台UI体验` | `feature/studio-ui-experience` | `8f2efea` | 工作台 UI、Puck、交互和浏览器体验 |
| `依赖安全升级` | `security/dependency-upgrades` | `9a557f6` | 已完成的生产依赖安全升级记录 |

## 3. 已完成里程碑与主要提交

| 提交 | 内容 |
|---|---|
| `63f66c7` | 建立 AppSpec 驱动的数据工作室原型 |
| `1b9d092` | 增加 AppSpec/ChangeSet Schema 与组件注册表 |
| `2ba6a8c` | 集成 Puck 可视化编辑器 |
| `8603e97` | 增加数据绑定和编辑器权限 |
| `0f80214` | 增加数据源可观察性、本地持久化并修复 Puck 状态循环 |
| `4cdc87f` | 集成 Schema 安全的 DeepSeek 规划 |
| `0747e65` | 增加可执行 DataRecipe 与字段血缘 |
| `7a0d45c` / `439d814` | 建立有界 DeepSeek Harness 执行流程 |
| `7729344` | 修正 Harness 模型、工具和总执行时间预算 |
| `d11e27e` | 优化复杂 Harness 任务上下文预算与 working memory |
| `52d4f28` | 增加安全的 DataRecipe Excel 导出 |
| `9a557f6` | 升级生产依赖并消除已报告的生产依赖审计问题 |
| `8f2efea` | 增加安全的临时 CSV 数据集接入 |

## 4. 当前系统能力

### 4.1 工作台与页面模型

- 三栏 DataCanvas AI 工作台。
- 页面结构树、桌面/移动预览和页面切换。
- AppSpec 递归渲染与类型化组件注册表。
- Puck 可视化编辑、组件拖拽和属性修改。
- viewer、editor、admin 演示权限。

### 4.2 ChangeSet

- Schema、目标、权限、页面、组件和属性校验。
- 预览、应用、拒绝、撤销和历史记录。
- 正式 AppSpec 与待确认预览隔离。
- localStorage 本地持久化与中文恢复错误。
- 审计记录只保存必要元数据。

### 4.3 数据处理

DataRecipe 已支持：

- 选择、筛选和重命名字段。
- 类型转换和派生字段。
- 分组、聚合、排序和行数限制。
- 输入/输出 Schema、步骤结果、耗时和中文错误。
- 字段血缘追踪、失败定位、局部撤销和结果恢复。

### 4.4 DeepSeek Harness

- 服务器端 DeepSeek 规划，客户端无法读取密钥。
- 状态包括 planning、executingTool、observing、awaitingConfirmation、completed、blocked、failed 和 cancelled。
- 支持任务复杂度分级、模型/工具次数、字符、token 和执行时间预算。
- 使用结构化 working memory，避免旧观察在后续轮次重复膨胀。
- 当前工具包括数据集检查、字段检查、配方预览/验证、ChangeSet 预览和 Excel 导出。
- 页面写入停在人工确认，不自动修改正式 AppSpec。

### 4.5 Excel 导出

- 依赖：`write-excel-file@4.1.1`，服务端使用 Node 导出入口。
- 导出 DataRecipe 执行后的结果，而非未经处理的原始数据。
- 生成真正的 `.xlsx` Buffer 和受控下载 API。
- 至少包含“分析结果”和“导出说明”工作表。
- 提供文件名清理、路径限制、公式注入防护、容量与超时限制。
- 导出内容不进入 localStorage、模型上下文或 ChangeSet 审计；审计只保存文件元数据。

### 4.6 CSV 临时数据集接入

- 服务端使用 `csv-parse@7.0.2`，解析器不进入客户端产物。
- 支持 UTF-8、UTF-8 BOM、引号、字段内逗号和换行。
- 处理空表头、重复表头、类型推断和字段映射。
- 提供空值、唯一值、重复行、类型冲突和异常值分析。
- 标记姓名、电话、邮箱、身份证和地址等敏感风险。
- 数据可供查询运行时、图表绑定、DataRecipe、Harness 和 Excel 导出使用。
- 敏感策略确认前，Harness 不向模型提供数据摘要；确认后只提供脱敏样本或排除敏感样本。

## 5. 当前依赖与运行环境

`site/package.json` 的已核对生产依赖：

- Node.js：`>=22.13.0`
- `next@16.3.4`
- `react@19.2.6`
- `react-dom@19.2.6`
- `@puckeditor/core@0.23.0`
- `zod@^4.5.4`
- `write-excel-file@4.1.1`
- `csv-parse@7.0.2`

常用命令（在 `site` 目录执行）：

```powershell
pnpm dev
pnpm test
pnpm lint
pnpm build
pnpm audit --prod
```

本地开发地址通常为：

```text
http://localhost:3000/
```

## 6. 最近验收结果

以下为 CSV 接入里程碑提交时的验收报告；Git 分支和提交状态已另外只读核对：

- 自动化测试：27 个测试文件、154 项全部通过。
- TypeScript、ESLint、生产构建和 `git diff --check` 通过。
- 首页冒烟：HTTP 200。
- 浏览器 Mock：上传、切换、字段分析、敏感策略确认、配方预览和下载按钮通过。
- `pnpm audit --prod`：Info、Low、Moderate、High、Critical 均为 0。
- 工作区、暂存区和提交内容敏感信息扫描：0 命中。
- 实际 CSV 文件、临时浏览器脚本、浏览器 profile、缓存和构建产物未进入提交。
- `.env.local` 被 Git 忽略且未跟踪；`.env.example` 中 DeepSeek 密钥为空。
- 未调用真实 DeepSeek API。

## 7. 安全与隐私约束

每轮开发必须继续遵守：

1. 不读取、打印、复制或提交 `.env.local` 的密钥内容。
2. `.env.example` 只能保留空的密钥占位符。
3. 禁止提交真实 CSV、真实姓名、电话、邮箱、身份证、地址或公司业务数据。
4. 测试数据只能使用明确的虚构值。
5. 禁止提交 `node_modules`、`.next`、`.vinext`、`.wrangler`、`dist`、缓存、临时浏览器目录和构建产物。
6. 每次提交前执行工作区、暂存区和最终提交对象的敏感信息扫描。
7. AI 只能看到经过策略允许的 Schema、摘要或脱敏样本。
8. 写入页面必须经过 ChangeSet 预览和人工确认。
9. 未完成认证与持久化改造前，不公开部署真实业务数据功能。

## 8. 已知限制

- 上传数据仅保存在服务端临时内存，最长 30 分钟、最多 10 个数据集；服务重启后失效。
- CSV 仅支持 UTF-8 与 UTF-8 BOM。
- 服务端解析阶段暂不提供逐行进度。
- 当前没有正式认证体系；短期数据集主要依赖随机 ID、TTL 和敏感策略隔离。
- 当前持久化以浏览器 localStorage 为主，不支持多用户同步。
- Excel 下载使用临时导出存储，不是长期文件仓库。
- 最新 CSV/Excel 能力完成了 Mock 和自动化验收，但尚未进行新的真实 DeepSeek 端到端验收。
- `main` 尚未接收功能分支代码，也尚未建立正式发布流程。

## 9. 双分支协作边界

### `feature/data-harness-platform`

主要负责：

- `site/core/`
- `site/app/api/`
- 数据集仓库、数据库适配和认证边界。
- Harness、DeepSeek 服务端、DataRecipe、导出和服务端测试。
- 公共 Schema、Harness contracts、`package.json` 和锁文件。

### `feature/studio-ui-experience`

主要负责：

- `site/components/`
- `site/app/globals.css`
- `StudioWorkspace.tsx`
- Puck 编辑体验、上传与历史界面、移动端和无障碍。
- 浏览器 UI 测试。

### 共享文件规则

- 公共 Schema、Harness contracts、依赖和锁文件默认由数据分支修改。
- UI 分支消费公共类型，确需修改时先同步双方任务边界。
- 集成期间不要直接在 `feature/website-redesign` 开发新功能。
- 两个子分支均不直接修改或合并 `main`。

## 10. 推荐的下一阶段

### 数据与 Harness 分支

优先级建议：

1. 将临时内存数据仓库抽象为可替换的持久化适配器。
2. 增加正式认证、租户和数据集所有权校验。
3. 建立真实数据源适配器与服务端权限策略。
4. 完成 CSV → DataRecipe → Harness → Excel 的受控真实端到端验收。

### 工作台 UI 分支

优先级建议：

1. 完善上传、字段映射、敏感确认和导出状态体验。
2. 完善 ChangeSet、Harness 和数据任务历史查看。
3. 完成移动端、键盘操作和无障碍验收。
4. 补充错误恢复、空状态和长任务进度展示。

## 11. 集成与提交检查清单

每个子分支提交前：

- [ ] 当前分支正确，工作范围符合分支责任。
- [ ] `git diff --check` 通过。
- [ ] 自动化测试、TypeScript、ESLint 和生产构建通过。
- [ ] `pnpm audit --prod` 无新增 High/Critical。
- [ ] `.env.local` 被忽略且未跟踪。
- [ ] `.env.example` 不包含密钥值。
- [ ] 工作区、暂存区和提交对象敏感信息扫描为 0 命中。
- [ ] 无真实数据、临时文件、缓存或构建产物进入提交。
- [ ] 只推送当前子分支，不操作 `main`，不自动创建 Pull Request。

集成顺序建议：

1. 子分支完成小里程碑并提交、推送。
2. 将数据分支的稳定提交合入 `feature/website-redesign`。
3. UI 分支同步最新集成分支并处理类型或 API 变化。
4. 将 UI 分支合入集成分支。
5. 在集成分支执行完整自动化、浏览器、隐私和真实低成本验收。
6. 达到发布标准后，再单独评审是否合并 `main`。

